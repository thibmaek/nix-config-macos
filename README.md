# nix-config-macos

My Nix config for macOS machines. Ideally merged with [thibmaek/nix-config-debian].

## Installing

1. Clone this to `~/.config/nix`
2. Install Nix (using determinate-installer)
3. Run `darwin-rebuild switch --flake ~/.config/nix` to activate
